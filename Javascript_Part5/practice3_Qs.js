// Qs2.Create an object representing a car that stores the following properties for the
// car: name, model, color.
// Print the car’s name.

let car = {
    carName: "BMW",
    carModel: "M4",
    carColor: ["black", "Darkgreen"],
}

console.log(car);
console.log(car.carName);
